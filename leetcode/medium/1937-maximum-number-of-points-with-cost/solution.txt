class Solution {
public:
    long long maxPoints(vector<vector<int>>& points) {
        int n = points.size();
        int m = points[0].size();

        vector<long long> previous_row(m, 0), cur(m, 0), left_max(m, 0), right_max(m, 0);
        for(int i = 0; i < n; i++) {
            
            left_max[0] = previous_row[0];
            right_max[m - 1] = previous_row[m - 1];
            for(int j = 1; j < m; j++) {
                left_max[j] = max(previous_row[j], left_max[j - 1] - 1);
                right_max[m - 1 - j] = max(previous_row[m - 1 - j], right_max[m - j] - 1);
            }

            for(int j = 0; j < m; j++) {
                cur[j] = poi