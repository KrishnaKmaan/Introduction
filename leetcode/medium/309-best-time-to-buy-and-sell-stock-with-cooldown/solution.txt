ld] != -1) return cache[i][hold];
        int profit = 0;
        if(hold == 0) {
            return cache[i][hold] = max(f(i + 1, 1) - prices[i], f(i + 1, 0));
        } else {
            return cache[i][hold] = max(f(i + 2, 0) + prices[i], f(i + 1, 1));
            // selling today and bcoz of cooldown i can't buy next day
        }
    }
    int maxProfit(vector<int>& prices) {
        this->prices = prices;
        cache.resize(prices.size() + 3, vector<int>(2, -1));
        return f(0, 0);
    }
};